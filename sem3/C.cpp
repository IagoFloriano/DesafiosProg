#include <bits/stdc++.h>
using namespace std;

using ll = long long;

int main() {
  ll a, b, n;
  cin >> a >> b >> n;

  // (b/temp) - ((a-1)/temp)

  vector<ll> primos;
  for (ll i=0; i < n; i++){
    ll temp;
    cin >> temp;
    primos.push_back(temp);
  }

  // tamanho do subconjunto
  for (ll i = 0; i < primos.size(); i++){
    if (
  }

}
